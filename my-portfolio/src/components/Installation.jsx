import React from 'react';

const Installation = () => {
  return (
    <div className="installation">
      <h3>Installation</h3>
      <p>To set up the project, follow the steps below:</p>
      <ol>
        <li>Clone the repository: `git clone https://github.com/your-repo/image-processing.git`</li>
        <li>Navigate to the project directory: `cd image-processing`</li>
        <li>Install dependencies: `pip install -r requirements.txt`</li>
        <li>Run the application: `python app.py`</li>
      </ol>
    </div>
  );
};

export default Installation;
    